# environments / caches
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
pytest-cache-files-*/
.ipynb_checkpoints/
.fastembed_cache/

# OS / editor junk
.DS_Store
*.tmp

# large / regenerable artefacts
data/qdrant_db/            # on-disk Qdrant (service mode); rebuilt from npz
data/pdfs/                 # source PDFs (regenerate: scripts/build_cache.py)
data/_embed_partial.npz   # resumable-embedding scratch file
scripts/_regen_embed.py   # local scratch helper (use scripts/build_embeddings.py)

# Keep (committed so the repo reproduces offline with no re-embedding / no Docker):
#   data/cache/*.jsonl  data/embeddings.npz  data/cites.json  data/gold_qa.json
#   src/  scripts/  api/  tests/  results/*  diagram/*  run_card.yaml  README.md  D3.ipynb
#   docker-compose.yml  .env.example

# node
node_modules/

# LibreOffice lock/temp
.~lock.*#
